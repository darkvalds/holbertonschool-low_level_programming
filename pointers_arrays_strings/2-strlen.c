#include "main.h"

/**
 *_strlen - Returns the lengh of a string.
 *@s: The string to get the length of.
 *
 * Return: The length of *str.
 */

int _strlen(char *s)
{
	int l;

	for (l = 0; s[l] ; l++)
	{
	}
	return (l);
}
